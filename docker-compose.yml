version: "3.9"
services:
  frontend_portfolio:
    container_name: frontend_portfolio
    image: frontend_portfolio
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    restart: unless-stopped
